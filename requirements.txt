# Core SASA / delta-SASA preprocessing uses only the Python standard library.
#
# Optional dependencies for the B + D extension:
# - torch: train MLP / GCN baselines and run ESM-2 models.
# - transformers: load HuggingFace ESM-2 models.
#
# Install them only when you need ESM embeddings or neural model training, for example:
# pip install torch transformers
